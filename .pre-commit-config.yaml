repos:
- repo: https://github.com/compilerla/conventional-pre-commit
  rev: v1.3.0
  hooks:
    - id: conventional-pre-commit
      stages: [commit-msg]
      args: [feat, chore, build, ci, refactor, docs] # optional: list of Conventional Commits types to allow
